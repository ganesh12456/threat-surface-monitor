version: '3.8'

services:
  # Unified Web Application (React + FastAPI)
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tsm_unified_app
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
